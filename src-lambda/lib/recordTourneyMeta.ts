import { iStatStore, iTournament } from "../../src-shared/types";
import { putTourneyMeta } from "./dynamo";


export const recordTourneyMeta = (tourney: iTournament, statStore: iStatStore) => {
        //putTourneyMeta()
}